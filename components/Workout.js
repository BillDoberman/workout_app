import React, { useState } from 'react';
import { Button } from 'react-native';

function Workout({ navigation }) {
  return (
    <Button
      title="this is the workout screen"
    />
  );
}

export default Workout;